# 以卡片为原子且由真实音频时长驱动视频

已接受。LLM 独立生成三套完整候选方案，每套由与 SKU 和事实来源绑定的卡片组成；每张卡片包含自身口播。逐卡 TTS 完成后，真实音频时长决定卡片时长，不以预估文案字数硬凑时间轴。

## Consequences

一张 SKU 可对应一至多张卡片，但不得为凑时长编造内容。第一版无字幕，用户只可编辑卡片口播、删除卡片和调整顺序；视觉版式由所选模板控制。
